server:
  port: 1234

spring:
  datasource:
    username: boyNextDoor
    password: DeepD4rkFantasy
    url: jdbc:mysql://rbq.damaoooo.cf:8546/industry_monitor?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=CST
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.example.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug